let num='123';
if(isNaN(num)){
    console.log(`The ${num} is ${typeof(num)}`);
    document.write(`The ${num} is ${typeof(num)}`);
}
else {
    console.log(`The ${num} is ${typeof(num)}`);
    document.write(`The ${num} is ${typeof(num)}`); 
}

